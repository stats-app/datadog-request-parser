DataDog Request Parser
======================

This library converts the raw JSON requests sent by the datadog-agent forwarder into PHP objects.
Currently only very basic metric information is extracted.